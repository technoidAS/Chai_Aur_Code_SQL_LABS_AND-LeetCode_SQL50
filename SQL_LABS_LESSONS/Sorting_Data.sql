--ORDER BY Ascending
select * from products order by price;

--ORDER BY Descending
select name, price from products order by price desc;

--ORDER BY Multiple Columns
select name, country, age from users order by country, age;

